version: 2
updates:
- package-ecosystem: pip
  directory: "/"
  schedule:
    interval: monthly
  open-pull-requests-limit: 10
  labels:
      - "python"
      - "dependencies"
  commit-message:
      prefix: "Python"
      include: "scope"
